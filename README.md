# AI Gracie Phase 1 — PR screenshots

Screenshots for the AI Gracie Phase 1 pull request (#50), produced by
`src/kt-gracie-frontend/e2e/gracie-ai.spec.ts` on `feat/ai-gracie-phase1`.

This branch has no shared history with the code and is never merged. It only holds
images so the PR can show them without adding binaries to `develop` or `main`.
Safe to delete once the PR is merged.
